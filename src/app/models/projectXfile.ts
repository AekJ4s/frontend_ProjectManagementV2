import FileUpload from "./fileUpload";
import Projects from "./project"

export default class ProjectXFiles {
    id = 0;
    projectId = Projects
    fileuploadId = File
    isDeleted = false 
    fileUpload:FileUpload|null = null;
    project = Projects
}